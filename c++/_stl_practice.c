// vector
vector<int> arr;
arr.push_back(10);

// stack
stack<int> s;
s.push(10);
s.top();
s.pop();

// queue
queue<int> q;
q.push(10);
q.front();
q.pop();

// priority_queue max heap
priority_queue<int> pq;
pq.push(10);
pq.top();
pq.pop();

// list
list<int> adj;
adj.push_back(10);
adj.front();
adj.pop_front();

// set
set<int> s;
s.insert(10);
s.erase(10);
s.find(10)!=s.end();

// map
map<int, int> m;
m[1] = 10;
m.count(1) == 1 ? exists : false
